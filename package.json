{
  "name": "wpe4ydpv2",
  "appid": "8001779591038449",
  "version": "1.0.0",
  "description": "WPE browser shell for YDP X7",
  "icon": "icon.png",
  "quickjs": {
    "version": "20200705",
    "bigNum": false
  },
  "simulator": {
    "path": "",
    "page": ""
  },
  "single-js-bundle": false,
  "scripts": {
    "start": "aiot-cli preview",
    "clean:falcon": "sh scripts/prepare_falcon_stage.sh",
    "sync": "sh scripts/sync_generated.sh",
    "build": "npm run clean:falcon && npm run sync && aiot-cli -p && sh scripts/generate_release_manifest.sh",
    "build:dev": "npm run clean:falcon && npm run sync && aiot-cli",
    "build:bin": "npm run clean:falcon && npm run sync && aiot-cli -q -p",
    "build:prod": "npm run clean:falcon && npm run sync && aiot-cli -c -q -p",
    "build:bin-nopack": "npm run clean:falcon && npm run sync && aiot-cli -q",
    "build:web": "aiot-cli build-web",
    "build:simulator": "aiot-cli simulator ./",
    "upload": "aiot-cli upload -n",
    "test": "sh scripts/check_repo.sh",
    "test:keyboard": "node scripts/test_keyboard_event.mjs",
    "test:display": "node scripts/test_display_resolver.mjs",
    "check": "aiot-cli check",
    "check:beta": "aiot-cli check -b"
  },
  "keywords": [
    "iot",
    "miniapp"
  ],
  "devDependencies": {},
  "dependencies": {
    "falcon-ui": "^2.0.2"
  }
}
