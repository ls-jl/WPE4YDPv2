{
  "name": "wpe4ydpv2",
  "appid": "8001779591038449",
  "version": "1.0.0",
  "description": "WPE browser shell for YDP X7",
  "icon": "icon.png",
  "quickjs": {
    "version": "20200705",
    "bigNum": false
  },
  "simulator": {
    "path": "",
    "page": ""
  },
  "single-js-bundle": false,
  "scripts": {
    "start": "aiot-cli preview",
    "sync": "sh scripts/sync_generated.sh",
    "build": "npm run sync && aiot-cli -p",
    "build:dev": "npm run sync && aiot-cli",
    "build:bin": "npm run sync && aiot-cli -q -p",
    "build:prod": "npm run sync && aiot-cli -c -q -p",
    "build:bin-nopack": "npm run sync && aiot-cli -q",
    "build:web": "aiot-cli build-web",
    "build:simulator": "aiot-cli simulator ./",
    "upload": "aiot-cli upload -n",
    "test:keyboard": "node scripts/test_keyboard_event.mjs",
    "check": "aiot-cli check",
    "check:beta": "aiot-cli check -b"
  },
  "keywords": [
    "iot",
    "miniapp"
  ],
  "devDependencies": {},
  "dependencies": {
    "falcon-ui": "^2.0.2"
  }
}
